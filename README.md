# 🚀 Persona - Современный мессенджер

![Version](https://img.shields.io/badge/version-2H2C1S-blue)
![Status](https://img.shields.io/badge/status-stable-green)
![Release](https://img.shields.io/badge/release-09.02.2026-orange)
![Era](https://img.shields.io/badge/era-Horse🐴-red)

Полностью переписанный мессенджер на **Go + React + TypeScript** с современным Glassmorphism дизайном.

## ✨ Особенности

- 💬 **Real-time сообщения** через WebSocket
- 🎨 **Glassmorphism дизайн** с blur эффектами и градиентами
- 🏝️ **Динамический остров** - минимизированные звонки и уведомления в стиле iPhone
- 🔐 **JWT аутентификация**
- 📱 **Адаптивный интерфейс**
- ⚡ **Высокая производительность** благодаря Go
- 🎯 **TypeScript** для типобезопасности
- 🌈 **Плавные анимации** с Framer Motion
- 📞 **Аудио/видео звонки** с минимизацией
- 🔔 **Умные уведомления** с превью сообщений

## 🛠️ Технологии

### Backend
- **Go 1.21+** - высокопроизводительный backend
- **Gorilla Mux** - роутинг
- **Gorilla WebSocket** - real-time коммуникация
- **SQLite** - встроенная база данных
- **JWT** - аутентификация
- **Cloudinary** - хранение изображений

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Framer Motion** - анимации
- **Zustand** - state management
- **Socket.IO Client** - WebSocket клиент

## 📦 Установка

### Требования
- Go 1.21+
- Node.js 18+
- SQLite (встроенная база данных)
- Docker (опционально)

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-repo/kvant-2.0.git
cd kvant-2.0
```

### 2. Настройка Backend

```bash
# Создать .env файл
cp .env.example .env

# Отредактировать .env (установить JWT_SECRET, CLOUDINARY_* и т.д.)

# Установить зависимости Go
go mod download

# Запустить сервер
go run cmd/server/main.go
```

### 3. Настройка Frontend

```bash
cd client

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev
```

### 4. База данных

SQLite база данных создаётся автоматически при первом запуске (`kvant.db`).
Миграции выполняются автоматически.

---

## 🤖 Инструкция для AI ассистентов

### Как запустить локальный dev сервер

**ВАЖНО:** На Windows с PowerShell могут быть проблемы с политикой выполнения скриптов.

#### Правильный способ запуска:

1. **Backend** (Go сервер):
```bash
# Использовать controlPwshProcess для фонового процесса
controlPwshProcess(action: "start", command: "go run cmd/server/main.go")
```

2. **Frontend** (Vite dev server):
```bash
# НЕ использовать PowerShell напрямую! Использовать cmd через executePwsh:
executePwsh(command: 'cmd /c "cd client && npm run dev"', ignoreWarning: true, timeout: 5000)
```

#### Почему именно так:

- **Backend** запускается через `controlPwshProcess` как фоновый процесс
- **Frontend** запускается через `cmd /c` чтобы обойти политику выполнения PowerShell
- Timeout 5000ms для frontend - это нормально, сервер продолжит работать в фоне
- НЕ использовать `npm run dev` напрямую в PowerShell - будет ошибка `UnauthorizedAccess`

#### Проверка статуса:

```bash
# Проверить запущенные процессы
listProcesses()

# Посмотреть вывод backend
getProcessOutput(processId: 4, lines: 30)

# Backend должен показать:
# "🚀 Server starting on port 8080"

# Frontend должен показать:
# "VITE v5.x.x ready in XXXms"
# "Local: http://localhost:5173/"
```

#### Остановка серверов:

```bash
# Остановить backend
controlPwshProcess(action: "stop", processId: 4)

# Frontend остановится автоматически при закрытии терминала
```

#### Типичные ошибки:

1. **"bind: Only one usage of each socket address"** - порт 8080 занят
   - Решение: остановить старый процесс через `controlPwshProcess(action: "stop")`

2. **"UnauthorizedAccess" при npm** - политика PowerShell блокирует npm
   - Решение: использовать `cmd /c "cd client && npm run dev"` вместо прямого `npm run dev`

3. **Frontend не запускается** - не использовать `controlPwshProcess` для npm
   - Решение: использовать `executePwsh` с `cmd /c` и timeout

## 🐳 Docker

```bash
# Запустить всё через Docker Compose
docker-compose up -d

# Приложение будет доступно на:
# Frontend: http://localhost:5173
# Backend: http://localhost:8080
```

## 📁 Структура проекта

```
kvant-2.0/
├── cmd/
│   └── server/
│       └── main.go              # Точка входа backend
├── internal/
│   ├── handlers/                # HTTP handlers
│   ├── models/                  # Data models
│   ├── database/                # Database logic
│   ├── websocket/               # WebSocket hub
│   └── middleware/              # Middleware
├── pkg/
│   └── utils/                   # Утилиты
├── client/
│   ├── src/
│   │   ├── components/          # React компоненты
│   │   ├── pages/               # Страницы
│   │   ├── services/            # API сервисы
│   │   ├── store/               # State management
│   │   └── App.tsx              # Главный компонент
│   ├── package.json
│   └── vite.config.ts
├── go.mod
├── .env.example
├── docker-compose.yml
└── README.md
```

## 🎯 API Endpoints

### Публичные
- `POST /api/register` - Регистрация
- `POST /api/login` - Вход
- `GET /api/health` - Health check

### Защищённые (требуют JWT)
- `GET /api/users/search` - Поиск пользователей
- `GET /api/users/:id` - Получить профиль
- `PUT /api/users/:id` - Обновить профиль
- `POST /api/users/:id/avatar` - Загрузить аватар
- `GET /api/messages/:userId` - Получить сообщения
- `GET /ws` - WebSocket подключение

## 🎨 Дизайн

Проект использует **Glassmorphism** дизайн:
- Прозрачные элементы с backdrop-blur
- Градиентные кнопки и акценты
- Плавные анимации и переходы
- Тёмная тема по умолчанию
- Адаптивная вёрстка

## 🚀 Roadmap

- [x] Базовая аутентификация
- [x] Real-time сообщения
- [x] Glassmorphism UI
- [x] Аудио/видео звонки (UI)
- [x] Загрузка файлов (изображения)
- [x] Реакции на сообщения
- [x] Mobile приложение (Android)
- [x] Оптимизация производительности
- [ ] Групповые чаты
- [ ] Каналы и серверы
- [ ] WebRTC звонки (функционал)
- [ ] Push уведомления
- [ ] Premium функции

## 📱 Мобильное приложение (Android)

### Быстрая сборка

**Требуется:** Java 11+ (рекомендуется Java 17 LTS)

```powershell
# Автоматическая сборка
.\build-android.ps1

# Установка на телефон
adb install Persona-optimized.apk
```

### Оптимизации производительности (v2H3C1D)

✅ Мемоизация компонентов (React.memo)  
✅ Упрощенные анимации (0.2s вместо 0.7s)  
✅ Оптимизированные blur эффекты (8px на мобильных)  
✅ GPU ускорение (will-change, translateZ)  
✅ FPS при скролле: 30-40 → 50-60  

**Подробнее:**
- `QUICK_START_ANDROID.md` - быстрый старт
- `BUILD_ANDROID_APP.md` - подробная инструкция
- `PERFORMANCE_OPTIMIZATIONS.md` - описание оптимизаций
- `TESTING_PERFORMANCE.md` - тестирование

### Установка Java 17

1. Скачайте: https://adoptium.net/temurin/releases/
2. Выберите: Version 17, Windows, x64, JDK
3. Установите и запустите `build-android.ps1`

## 📝 Лицензия

MIT

## 👨‍💻 Автор

Квант © 2025
