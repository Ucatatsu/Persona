# Деплой фронтенда на Render - Пошаговая инструкция

## ✅ Что уже сделано
1. Исправлена ошибка `ReferenceError: Cannot access 'cr' before initialization`
2. Убрана circular dependency в backgroundStore
3. Код собран и запушен на GitHub

## 📋 Шаги для деплоя

### Шаг 1: Открыть Render Dashboard
Перейдите на: https://dashboard.render.com

### Шаг 2: Найти фронтенд сервис
Найдите сервис `persona-client-kcnt` в списке ваших сервисов

### Шаг 3: Проверить настройки сервиса

Нажмите на сервис и перейдите в Settings. Убедитесь, что указано:

**Build & Deploy:**
- **Build Command**: 
  ```
  cd client && npm install && npm run build
  ```
- **Publish Directory**: 
  ```
  client/dist
  ```

**Environment Variables:**
Добавьте/проверьте переменные окружения:
- `VITE_API_URL` = `https://persona-backend-o96b.onrender.com`
- `VITE_WS_URL` = `wss://persona-backend-o96b.onrender.com`

### Шаг 4: Включить Auto-Deploy (опционально)

В разделе Settings → Build & Deploy:
- Включите "Auto-Deploy" для автоматического деплоя при push в GitHub

### Шаг 5: Запустить деплой

**Вариант A: Автоматический (если Auto-Deploy включен)**
- Render автоматически начнет деплой после push
- Просто подождите 2-5 минут

**Вариант B: Ручной деплой**
1. Нажмите кнопку "Manual Deploy" в правом верхнем углу
2. Выберите "Deploy latest commit"
3. Дождитесь завершения (2-5 минут)

### Шаг 6: Проверить деплой

1. Откройте https://persona-client-kcnt.onrender.com
2. Нажмите F12 для открытия DevTools
3. Перейдите на вкладку Console
4. Обновите страницу (Ctrl+F5)

**Что должно быть:**
- ✅ Страница загружается без ошибок
- ✅ WebSocket подключается к `wss://persona-backend-o96b.onrender.com/api/ws`
- ✅ Нет ошибок про `localhost`
- ✅ Нет ошибок `ReferenceError: Cannot access 'cr'`

**Чего НЕ должно быть:**
- ❌ `Failed to load resource: 401`
- ❌ `WebSocket connection to 'ws://localhost:8080' failed`
- ❌ `ReferenceError: Cannot access 'cr' before initialization`
- ❌ Черный экран без интерфейса

### Шаг 7: Очистить кэш браузера

После успешного деплоя:
1. Нажмите Ctrl+Shift+Delete
2. Выберите "Cached images and files"
3. Нажмите "Clear data"
4. Обновите страницу (Ctrl+F5)

## 🔍 Проверка логов

Если что-то не работает:

1. В Render Dashboard откройте ваш сервис
2. Перейдите на вкладку "Logs"
3. Проверьте логи сборки на наличие ошибок

**Типичные ошибки:**
- `npm ERR!` - проблема с установкой зависимостей
- `Build failed` - ошибка компиляции
- `Command not found` - неправильная команда сборки

## 🆘 Если деплой не работает

### Проблема: Build Command не найден
**Решение:** Убедитесь, что Build Command точно:
```
cd client && npm install && npm run build
```

### Проблема: Publish Directory пустая
**Решение:** Убедитесь, что Publish Directory точно:
```
client/dist
```

### Проблема: Переменные окружения не работают
**Решение:** 
1. Проверьте, что переменные добавлены в Settings → Environment
2. После добавления переменных нужно сделать Manual Deploy

### Проблема: Сайт показывает старую версию
**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Откройте в режиме инкогнито
3. Проверьте в DevTools → Network, что файлы загружаются с Render

## 📱 После деплоя фронтенда

Когда фронтенд задеплоен, можно:
1. Тестировать веб-версию на https://persona-client-kcnt.onrender.com
2. Использовать мобильное приложение `Persona-FIXED-WEBSOCKET.apk`

Оба будут работать с одним бэкендом: https://persona-backend-o96b.onrender.com

## Дата
28 февраля 2026, 19:30
