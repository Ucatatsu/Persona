# Исправление ошибки входа (401 Unauthorized)

## Проблема
Пользователи получают ошибку 401 (Unauthorized) при попытке входа в систему и загрузке ресурсов.

## Причина
Отсутствовала переменная окружения `JWT_SECRET` в конфигурации Render, что приводило к использованию fallback секрета и невозможности верификации токенов.

## Исправления

### 1. Добавлен JWT_SECRET в render.yaml
```yaml
envVars:
  - key: JWT_SECRET
    generateValue: true
```

### 2. Добавлены VAPID ключи для push-уведомлений
```yaml
  - key: VAPID_PUBLIC_KEY
    value: BDd3hdZyXYibDIQqf_XtardGtmqaELw5_aO6WFWKFT8iLp4IcrIanZqOCsJrKlOh0BchoGdm8mFh9TbT3kRXtA0
  - key: VAPID_PRIVATE_KEY
    value: WlsEpZoei1Rcrtrzve2PmL9IoFbNalQ6JiEMQmCj2lk
```

### 3. Улучшено логирование
- Добавлено логирование переменных окружения при запуске
- Детальное логирование процесса аутентификации (только в development)
- Логирование попыток входа и генерации токенов

### 4. Добавлен тестовый endpoint
`/api/test-auth` - для проверки работы аутентификации

## Инструкции по деплою

### 1. После деплоя проверьте логи:
Ищите сообщения:
```
🔧 Переменные окружения:
   JWT_SECRET: установлен
```

### 2. Тестирование:
- Откройте `/api/test-auth` с валидным токеном
- Попробуйте войти в систему
- Проверьте загрузку стикеров

### 3. Если проблема остается:
1. Проверьте, что Render сгенерировал JWT_SECRET
2. Перезапустите сервис в Render
3. Очистите localStorage в браузере и попробуйте войти заново

## Диагностика

### Проверка переменных окружения:
```bash
curl https://your-app.onrender.com/api/test-db
```

### Проверка аутентификации:
```bash
# Сначала войдите и получите токен
curl -X POST https://your-app.onrender.com/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"your_username","password":"your_password"}'

# Затем проверьте токен
curl https://your-app.onrender.com/api/test-auth \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Возможные проблемы

1. **JWT_SECRET не сгенерирован**: Render должен автоматически сгенерировать значение
2. **Старые токены**: Пользователям нужно будет войти заново
3. **Кэш браузера**: Очистите localStorage и cookies
4. **Сетевые проблемы**: Проверьте доступность сервера

## Логи для поиска

В логах Render ищите:
- `🔧 Переменные окружения`
- `🔐 Login attempt for user`
- `✅ Login success`
- `❌ Auth failed`
- `🔑 Token verified`